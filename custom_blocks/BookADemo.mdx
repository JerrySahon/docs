---
name: BookADemo
---
export const BookDemoButton = ({
  href = "https://calendly.com/all_pearch/hrachik-and-jerry-15min",
  label = "Book a demo",
  sublabel = "15 minutes • Calendly",
  size = "lg",    // sm | md | lg
  icon = true
}) => {
  const sizes = {
    sm: "text-sm px-4 py-2.5",
    md: "text-base px-5 py-3",
    lg: "text-lg px-6 py-4"
  }[size] || "text-lg px-6 py-4";

  return (
    <div className="flex justify-center w-full">
      <a
        href={href}
        target="_blank"
        rel="noopener noreferrer"
        className={[
          "group flex items-center gap-3 rounded-2xl shadow-lg transition-all duration-200",
          "bg-[#2a2a2a] text-white hover:bg-black",               // светлая тема
          "dark:bg-white dark:text-[#2a2a2a] dark:hover:bg-gray-100", // тёмная тема
          "focus:outline-none focus:ring-4 focus:ring-black/30 dark:focus:ring-white/40",
          "w-[70%] justify-center", // ширина 70% от контейнера
          sizes
        ].join(" ")}
      >
        {icon && (
          <i className="fa-solid fa-calendar text-xl group-hover:scale-105 transition-transform" />
        )}
        <span className="leading-tight text-center">
          <span className="font-semibold">{label}</span>
          {sublabel && (
            <span className="block opacity-80 text-xs font-normal">
              {sublabel}
            </span>
          )}
        </span>
        <i className="fa-solid fa-arrow-right text-lg opacity-80 group-hover:translate-x-0.5 transition-transform" />
      </a>
    </div>
  );
};